fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0
sentry-sdk[fastapi]==1.45.0
sqlalchemy==2.0.23
alembic==1.12.1
psycopg[binary]==3.3.4
psycopg2-binary==2.9.9
pydantic-settings==2.1.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-multipart==0.0.6
redis==5.0.1
celery==5.3.4
email-validator==2.1.0
python-docx==0.8.11
pypdf==4.0.1
# CV text extraction + OCR (scanned PDFs and image CVs).
# PyMuPDF ships musllinux wheels (Alpine-compatible); pytesseract shells out to
# the tesseract binary installed in the Docker image; Pillow rasterises images.
pymupdf==1.24.10
pytesseract==0.3.13
Pillow==10.4.0
# Rules-based CV parser: fuzzy field matching + language detection.
rapidfuzz==3.9.3
langdetect==1.0.9
# ATS CV generator: PDF export (DOCX already covered by python-docx above).
reportlab==4.2.2
# HTML/CSS-driven resume rendering (Phase B) — needs pango/fontconfig at
# runtime (see Dockerfile's runtime stage); pip install itself doesn't
# require them since weasyprint loads them via cffi at import time, not
# link time. Optional at the app level too — gated behind
# RESUME_WEASYPRINT_ENABLED, falls back to reportlab's to_pdf() above.
weasyprint==69.0
slowapi==0.1.9
jinja2==3.1.2
python-dotenv==1.0.0
httpx==0.25.2
requests==2.31.0
aiofiles==23.2.1
boto3==1.34.69
minio==7.1.0
